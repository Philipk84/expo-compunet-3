{
  "name": "bitacora-cero-senal",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c yellow,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "check": "npm run check -w client && npm run check -w server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
